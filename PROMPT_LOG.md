# PROMPT_LOG.md

## Задание 1: Разработка сборщика данных на Go

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты — senior Python разработчик. Тема «Сбор и анализ сельхозданных», источник —
датчики полей и метеостанции. Реализуй на Go программу-сборщик данных.
Используй горутины для параллельного сбора: 5 полей (датчики — температура, влажность,
влажность почвы, NPK, pH) и 5 метеостанций (температура, давление, ветер, осадки,
солнечная радиация). Данные — 168 часов (неделя). Сохранять в NDJSON-файлы (один объект
на строку). Без комментариев в коде.

**Что сделано:**
1. Создан `collector/main.go` — два типа источников: `SensorReading` (поля) и `WeatherReading`
   (метеостанции), реалистичная генерация с синусоидальным суточным профилем температуры
   и нормальным/экспоненциальным шумом
2. Горутины: по одной на каждое поле и каждую станцию (10 горутин параллельно),
   синхронизация через `sync.WaitGroup`
3. Запись через `json.NewEncoder` — один объект на строку (NDJSON)
4. Создан `collector/go.mod`
5. Результат: 10 файлов в `data/`, 1680 записей, выполнение за ~8ms

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 2: Добавление буферизации и пакетной записи

**Инструмент:** Claude (claude.ai)

**Промпт:**
Модифицируй сборщик Go из задания 1: добавь канал с буфером и пакетную запись.
Накапливать N=20 записей или ожидать T=2s перед записью в файл.
Цель — снизить количество операций ввода-вывода. Без комментариев в коде.

**Что сделано:**
1. Добавлена структура `BatchWriter` — инкапсулирует канал, горутину-воркер и статистику
2. Метод `loop()` читает из канала и флашит батч при двух условиях:
   - накоплено `batchSize=20` записей (немедленный flush)
   - сработал `time.Ticker` с `flushTimeout=2s` (flush по таймауту)
3. При закрытии канала (`bw.Close()`) — финальный flush остатка
4. Буфер канала = `batchSize*2` — исключает блокировку продюсеров
5. Результат: 168 записей → 9 флашей вместо 168 операций записи (экономия ~95%)

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 3: Реализация graceful shutdown

**Инструмент:** Claude (claude.ai)

**Промпт:**
Добавь в Go-сборщик обработку сигналов SIGINT/SIGTERM. При завершении сборщик должен
дообработать текущие данные, записать буфер в файл и корректно закрыть соединения.
Без комментариев в коде.

**Что сделано:**
1. Добавлен `context.WithCancel` — контекст передаётся в `collectSensorData` и
   `collectWeatherData`, оба цикла проверяют `ctx.Done()` на каждой итерации
2. Горутина-слушатель перехватывает `os.Signal` через `signal.Notify` на `SIGINT`/`SIGTERM`
   и вызывает `cancel()`
3. При прерывании каждая горутина выходит из цикла, передаёт уже накопленные записи
   в `BatchWriter`, затем `bw.Close()` выполняет финальный flush буфера
4. `sync.WaitGroup` гарантирует ожидание всех горутин перед выходом из `main`
5. Финальное сообщение различается: graceful shutdown vs нормальное завершение

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 4: Импорт данных в Python (Polars)

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты — senior Python разработчик. Тема «Сбор и анализ сельхозданных». Напиши Python-скрипт
pipeline.py, который загружает NDJSON-файлы из папки data/ в DataFrame Polars (отдельно
датчики полей sensors_*.json и метеостанции weather_*.json). Выведи первые 5 строк и
базовую информацию: типы колонок, количество строк, количество пропусков. Без комментариев
в коде.

**Что сделано:**
1. Функции `load_sensors()` и `load_weather()` — glob по `data/`, `pl.read_ndjson`,
   `pl.concat`
2. `task4_import()` выводит `.shape`, `.head(5)`, `.schema`, `.null_count()` для обоих
   DataFrame
3. Точка входа `if __name__ == "__main__"` вызывает только `task4_import()`

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 5: Очистка и валидация данных

**Инструмент:** Claude (claude.ai)

**Промпт:**
Дополни pipeline.py: добавь функцию task5_clean(sensors, weather), которая выполняет
очистку данных через Polars. Шаги: удалить дубликаты, привести timestamp к Datetime,
заполнить пропуски медианой по каждой числовой колонке, отфильтровать выбросы
(температура -50..60°C, влажность 0..100%, pH 0..14, давление 800..1100 hPa).
Документировать каждый шаг через print. Без комментариев в коде.

**Что сделано:**
1. `unique()` — удаление дубликатов, вывод счётчика удалённых строк
2. `cast(pl.Datetime("us", "UTC"))` — приведение типа `timestamp`
3. Заполнение пропусков медианой для всех числовых колонок обоих DataFrame
4. `.filter()` с `is_between()` — удаление физически невозможных значений
5. `task4_import()` → `task5_clean()` в `main`

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 6: Агрегационный анализ

**Инструмент:** Claude (claude.ai)

**Промпт:**
Дополни pipeline.py: добавь функцию task6_aggregate(sensors, weather). Выполни
группировку датчиков по полю field_id и метеостанций по region. Вычисли агрегаты:
AVG, MIN, MAX для температуры, SUM для осадков и азота, COUNT записей. Выведи результат
в табличном виде. Без комментариев в коде.

**Что сделано:**
1. `sensors.group_by("field_id")` — AVG/MIN/MAX температуры, AVG влажности и почвы,
   AVG pH, SUM азота, COUNT
2. `weather.group_by("region")` — AVG температуры и давления, SUM осадков, MAX ветра,
   AVG солнечной радиации, COUNT
3. Оба результата отсортированы и выведены через `print()`
4. `main` обновлён: `task6_aggregate(sensors_clean, weather_clean)`

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---


## Задание 7: Сохранение в Parquet

**Инструмент:** Claude (claude.ai)

**Промпт:**
Дополни pipeline.py: добавь функцию task7_parquet(sensors, weather), которая сохраняет
очищенные DataFrame в формат Parquet. Сенсоры — в data/agro_clean.parquet, метеостанции —
в data/weather_clean.parquet. Вывести путь и размер каждого файла. Без комментариев
в коде.

**Что сделано:**
1. `sensors.write_parquet("data/agro_clean.parquet")`
2. `weather.write_parquet("data/weather_clean.parquet")`
3. Вывод пути и `os.path.getsize()` в байтах для каждого файла
4. `main` обновлён: вызов `task7_parquet` после агрегации

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~3 мин

---

## Задание 8: Анализ через DuckDB (SQL)

**Инструмент:** Claude (claude.ai)

**Промпт:**
Дополни pipeline.py: добавь функцию task8_duckdb(). Выполни SQL-запрос к
data/agro_clean.parquet через DuckDB — с фильтрацией WHERE, GROUP BY field_id и
ORDER BY. Тот же запрос воспроизведи в Polars. Замерь время выполнения обоих вариантов
через time.perf_counter и сравни результат. Без комментариев в коде.

**Что сделано:**
1. SQL: `SELECT field_id, AVG(temperature_c), AVG(humidity_pct), AVG(soil_moisture_pct),
   AVG(ph), COUNT(*) FROM read_parquet(...) WHERE temperature_c > 10 AND humidity_pct > 40
   GROUP BY field_id ORDER BY avg_temp DESC`
2. Эквивалентный запрос на Polars: `.filter().group_by().agg().sort()`
3. Замер `time.perf_counter()` для обоих, вывод в миллисекундах и коэффициент разницы
4. `main` обновлён: `task8_duckdb()` вызывается после сохранения Parquet

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---